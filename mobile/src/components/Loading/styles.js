import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    loadingContainer: {
        width: '100%',
        flex: 1,
        alignContent: 'center',
        justifyContent: 'center',
    },

    image: {
        height: 50,
        width: 50,
        alignSelf: 'center',
    }
})